<%= form_with url: "/merchant/discounts/#{@discount.id}/update", model: @discount do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :quantity %>
  <%= f.number_field :quantity %>

  <%= f.label :discount %>
  <%= f.number_field :discount %>

  <%= f.submit %>
<% end %>
